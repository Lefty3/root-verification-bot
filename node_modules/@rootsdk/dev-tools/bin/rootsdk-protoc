#!/usr/bin/env node

const { runRootProtoc } = require("../dev-app/dist/rootprotoc");

try
{
    runRootProtoc();
}
catch
{
    process.stderr.write('failed to run rootplatform-protoc');
    process.exit(1);
};
