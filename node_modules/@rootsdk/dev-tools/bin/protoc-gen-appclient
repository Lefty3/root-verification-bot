#!/usr/bin/env node

const { RootTsAppPlugin } = require("../dev-app/dist/rootTsAppPlugin");

new RootTsAppPlugin().run().catch(_ => {
    process.stderr.write('failed to run plugin');
    process.exit(1);
});
